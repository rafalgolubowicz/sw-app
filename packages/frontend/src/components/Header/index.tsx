import { Layout, Typography } from 'antd';

const Header = () => (
  <Layout.Header>
    <Typography.Title className="text-white">SW APP</Typography.Title>
  </Layout.Header>
);

export default Header;
